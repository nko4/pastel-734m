extends layout
 
block script
  script
    S.pair(1, function(socket, master) {
      conn = socket.conn
      conn.on('data', function(data) { console.log(data) });
      if (master) {
        for (var i = 0; i < 1; i++)
          conn.send({ n: i, payload: Array(20000) });
        for (var i = 0; i < 100; i++)
          conn.send({ n: i, payload: Array(2) });
        for (var i = 0; i < 1; i++)
          conn.send({ n: i, payload: Array(3000) });
      }
    });

